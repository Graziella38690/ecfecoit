{% extends 'base.html.twig' %}

{% block title %}Liste des utilisateurs!{% endblock %}

{% block body %}

<table class="table ">
       
        
            <tbody>
                {% for user in user %}

                <tr>
                <th> Email </th>
                <td> {{ user.email }} </td>
                </tr>
                <tr>
                <th> Pseudo </th>
                <td> {{ user.Pseudo }} </td>
                </tr>

                <tr>
                <th> Prenom </th>
                <td> {{ user.Firstname }} </td>
                </tr>
    
                <tr>
                <th> Nom </th>
                <td> {{ user.Lastname }} </td>
                </tr>
                <tr>
                <th> Rôles </th>
                <td> {% for roles in user.roles %}
                {% if roles == 'ROLE_TEACHERWAIT' %}
                 Instructeur en attente
                 {% endif %}
                {% if roles == 'ROLE_TEACHER' %}
                 Instructeur
                {% endif %}
                {% if roles == 'ROLE_LAERNING' %}
                Apprenant
     {% endif %}</td>
    {% endfor %}     
   

    <a href="{{ path('app_edit_user', {'id': user.id}) }}" class="btn btn-warning" role="button">Modifier</a>      
    {% endfor %}
               
            </tbody>
        </table>
       
        {% endblock %}