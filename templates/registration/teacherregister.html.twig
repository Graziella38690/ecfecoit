{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<div class="container-fluid lg-container">
    <div class="row vh-100 d-flex justify-content-center align-items-center">
        <div class="col-10 col-lg-4" id="registerForm">
    <h1>Register</h1>
{{ form_start(teacherForm) }}

   
        {{ form_row(teacherForm.pseudo,{
            label:'Pseudo'
        }) }}
        {{ form_row(teacherForm.firstname,{
            label:'Prenom'
        }) }}
        {{ form_row(teacherForm.lastname,{
            label:'Nom'
           }) }}
      
        {{ form_row(teacherForm.email,{
            label:'E-mail'
           }) }}
        {{ form_row(teacherForm.plainPassword, {
            label: 'Mot de passe'
        }) }}

        {{ form_row(teacherForm.photo, {
            label: 'Photo'
        }) }}


        {{ form_row(teacherForm.agreeTerms, {
            
        label:'En m\'inscrivany à ce site j\'accepte...'
        }) }}

 
        <button class="btn btn-lg btn-vine" type="submit">Postuler
    {{ form_end(teacherForm) }}
        </div>
    </div>
</div>
{% endblock %}

