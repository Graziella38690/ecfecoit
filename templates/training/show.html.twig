{% extends 'base.html.twig' %}

{% block title %}Training{% endblock %}

{% block body %}
 <div class="conteneur">
    <h1>Training</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ training.Title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ training.Description }}</td>
            </tr>
            <tr>
                <th>Catchphrase</th>
                <td>{{ training.catchphrase }}</td>
            </tr>
            <tr>
                <th>Datecreate</th>
                <td>{{training.Datecreate|date('Y-m-d')}}</td>
            </tr>
         
         
            <tr>
              
                <th> Section </th>
                <td>
                 {% for section in training.sections %}
               <li> {{section.Title}}</li>
                {% endfor %}
                
                </td>
           
            </tr>

 


         
			   
 
        </tbody>
    </table>

    <a href="{{ path('app_training_index') }}">back to list</a>

    <a href="{{ path('app_training_edit', {'id': training.id}) }}">edit</a>

    {{ include('training/_delete_form.html.twig') }}
    </div>
{% endblock %}
 