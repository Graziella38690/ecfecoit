{% extends 'base.html.twig' %}

{% block title %}Lesson{% endblock %}

{% block body %}



<div class="container-fluid container-lg text-center">
<div class="row">
      <div class="col-xs-12" id="title">

    
    
    
     
      
       <a href="{{ path('app_section_show',{'id': section.id}) }}"class="btn btn-Vine" role="button">Retour</a>
      
        <h1>Détail de la leçon n° {{lesson.Id }}</h1>
      </div>
      </div>
    <div class="row">
      <div class="col-xs-12">
       <h2> Titre :</h2> <h4> {{ lesson.Title }}</h4>
      </div>
    </div>


 <div class="row">
      <div class="col-xs-12">
       <h3> Résumer:</h3><h4>{{ lesson.textlesson }}</h4>
      </div>
    </div>

<div class="row">
      <div class="col-xs-12">
       <h3>Fait partit de la section:</h3>
       <h4>{{ lesson.containedIn }}</h4>
      </div>
    </div>


    <div class="row">
      <div class="col-xs-12">
       <h3>Document:</h3>
       <h4>{{ lesson.ressources }}</h4>
        </div>   
      
    </div>
   
  


    


           {% for ressource in lesson.ressources %}
  <pdf src="{{ asset('uploads/resource/'  ~ lesson.ressources) }}" class="img-training" alt="imageforamtion" height="250" width="250">
           
    <a href="{{ asset('uploads/resources/' ~ lesson.resources) }}">View brochure (PDF)</a>
     <iframe src="{{ asset('uploads/resources/' ~ lesson.ressources ) }}" width="100%" height="100%" > </iframe>

  
 

    
   
    {% endfor %}
 

  

{% endblock %}



 

 

