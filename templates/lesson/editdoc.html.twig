{% extends 'base.html.twig' %}

{% block title %}Edit Leçon{% endblock %}

{% block body %}
 <div class="container-fluid container-lg ">
 <div class="col-xs-12 text-center ">
    <h1>Modifier la lesson </h1>
   
    <a href="{{ path('app_lesson_index') }}"class="btn btn-Vine">Retour</a>

<div class="row">
      <div class="col-xs-12">
       <h3>Documents présents</h3>
       <h5>{{ lesson.ressources ? lesson.ressources|join(', ') : '' }}</h5>
     
      </div>
    </div>
    {{ include('lesson/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('lesson/_delete_form.html.twig') }}
     </div>
       </div>
     
{% endblock %}
