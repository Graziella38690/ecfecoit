{% extends 'base.html.twig' %}

{% block title %}Modification de l'utilisateur{% endblock %}

{% block body %}
<div class = "container">
<h1 class= "mt-4" >Modifier ma photo</h1>
<div class="row">
      <div class="col-xs-12 text-center ">
      {% if app.user.photo != NULL %}
            <div><img src="{{ asset('uploads/images/'~ app.user.Photo) }}" class="card.img-avatar" alt="avatar" height="200" width="200"> 
            </div>
            {% endif %} 
            {% if app.user.photo == NULL %}
             <div><img src="{{ asset('picture/blankprofile.png') }}" class="card-img-avatar" alt="avatarblanc" height="200" width="200"> </div>
            {% endif %} 
      </div>
      </div>
</div>
{{ include('/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
{% endblock %}


