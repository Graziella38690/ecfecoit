  
    


          <nav class=" navbar navbar-expand-lg navbar-light ">
            <div class="container-fluid">
              
                
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarCollapse">
                    <ul class="navbar-nav me-auto mb-2 mb-md-0">
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{path('app_home')}}">ACCUEIL</a>
                        </li>
                        
                     
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{path('app_training_index')}}">NOS FORMATIONS</a>
                        </li>
                     
                    </ul>
                    
                    

                    <div class="text-end">
                      {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                     Connecte en tant que {{app.user.pseudo}}
                                    <a href="{{ path('app_profile',{id : app.user.id})}}"class="btn btn-Vine" role="button">Mon compte</a>
                     {% if is_granted('ROLE_TEACHER') %}      

                                  <a href="{{path('app_teacher_index')}}" class="btn btn-Vine"role="button">Gestion formations</a>
                                  {% endif %}
                     {% if is_granted('ROLE_ADMIN') %}      
                                  <a href="{{path('app_admin')}}"class="btn btn-Vine" role="button">Gestion utilisateurs</a>
                                  {% endif %}

                      {% if is_granted('ROLE_LEARNING') %}      

                           
                    {% endif %}  
                                 <a href="{{path('app_logout')}}"class="btn btn-Vine " role="button">Deconection</a>
                        {% else %}
                          <a href="{{path('app_login')}}"><button type="button" class="btn btn-Vine ">Connexion</button></a>
                              <a href="{{path('app_register')}}"><button type="button" class="btn btn-Vine ">S'inscrire</button></a>
                               
                               
                        {% endif %}
                   

                       
                    </div>
                </div>
           
        </nav>